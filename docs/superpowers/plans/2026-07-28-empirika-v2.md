# Empirika Website v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** One-page cinematográfica bilíngue (PT `/`, EN `/en`) da Empirika com hero scroll-world (vídeo Higgsfield scrubado), seções com parallax GSAP, form de contato via Resend e footer com CNPJ/endereço.

**Architecture:** Site estático Astro 5 com uma rota on-demand (`/api/contact`) via adapter Vercel. Componentes Astro por seção recebem `locale` e leem dicionários JSON. Animações em um módulo GSAP único; hero usa o scrub-engine da skill scroll-world com fallback de poster.

**Tech Stack:** Astro 5, Tailwind 4 (@tailwindcss/vite), GSAP ScrollTrigger, Resend, Vitest, @fontsource-variable/archivo + @fontsource/ibm-plex-mono, skill scroll-world (Higgsfield CLI + ffmpeg), skill impeccable.

## Global Constraints

- PT na raiz `/`; EN em `/en/`. Alternates `hreflang` no `<head>` de ambas.
- Footer exato: `CNPJ 66.436.862/0001-70` e endereço `Avenida Rio Branco 139 / Recife, Recife — PE / 50030-310` (endereço SÓ no footer).
- Fundo preto `#000`; texto branco `#fff`; cinza `#9c9c9c`; hairlines `rgb(255 255 255 / 0.16)`.
- `RESEND_API_KEY` apenas via env; nunca commitada. `.env` no `.gitignore`.
- `prefers-reduced-motion: reduce` → sem scrub/parallax; poster estático + fades.
- Sem tracking/analytics/cookies. Scroll nativo sempre (sem hijacking).
- E-mail destino do form: env `CONTACT_TO` (default `paulinhoerry@gmail.com`); remetente `CONTACT_FROM` (default `Empirika <onboarding@resend.dev>` até verificar domínio no Resend).
- Commits frequentes, mensagens `feat:`/`fix:`/`docs:` com trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Scaffold Astro + Tailwind + tokens

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `.gitignore`, `src/styles/global.css`, `src/pages/index.astro` (stub), `public/favicon.svg`
- Restore: `.agents/skills/impeccable/` (do `main`)

**Interfaces:**
- Produces: tokens Tailwind `bg-ink`, `text-paper`, `text-mute`, `border-line`, `font-display`, `font-mono`; comando `npm run build` verde.

- [ ] **Step 1: Restaurar a skill impeccable e criar .gitignore**

```bash
git restore --source=main -- .agents
```

`.gitignore`:
```
node_modules/
dist/
.astro/
.env
.env.*
.vercel/
```

- [ ] **Step 2: Criar package.json e instalar**

```json
{
  "name": "empirika-website",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run"
  }
}
```

Run: `npm install astro@^5 gsap@^3.13 resend@^4 @astrojs/vercel@^8 tailwindcss@^4.1 @tailwindcss/vite@^4.1 @fontsource-variable/archivo @fontsource/ibm-plex-mono && npm install -D vitest@^3 @types/node`

- [ ] **Step 3: astro.config.mjs, tsconfig.json, global.css**

`astro.config.mjs`:
```js
import { defineConfig } from 'astro/config';
import vercel from '@astrojs/vercel';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  output: 'static',
  adapter: vercel(),
  vite: { plugins: [tailwindcss()] },
});
```

`tsconfig.json`:
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "src/**/*"],
  "exclude": ["dist"]
}
```

`src/styles/global.css`:
```css
@import 'tailwindcss';
@import '@fontsource-variable/archivo';
@import '@fontsource/ibm-plex-mono';

@theme {
  --color-ink: #000000;
  --color-paper: #ffffff;
  --color-mute: #9c9c9c;
  --color-line: rgb(255 255 255 / 0.16);
  --font-display: 'Archivo Variable', system-ui, sans-serif;
  --font-mono: 'IBM Plex Mono', ui-monospace, monospace;
}

html { background: var(--color-ink); color: var(--color-paper); scroll-behavior: smooth; }
@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto; } }
body { font-family: var(--font-display); -webkit-font-smoothing: antialiased; }
```

- [ ] **Step 4: Stub index + favicon e verificar build**

`src/pages/index.astro`:
```astro
---
import '../styles/global.css';
---
<html lang="pt-BR"><head><meta charset="utf-8" /><title>Empirika</title></head>
<body class="bg-ink text-paper"><h1 class="font-display text-6xl">Empirika</h1></body></html>
```

`public/favicon.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" fill="#000"/><text x="16" y="22" font-family="monospace" font-size="18" fill="#fff" text-anchor="middle">E</text></svg>
```

Run: `npm run build` — Expected: build verde, aviso nenhum de adapter.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: scaffold astro 5 + tailwind 4 with dark editorial tokens"
```

---

### Task 2: i18n — dicionários e helper

**Files:**
- Create: `src/i18n/pt.json`, `src/i18n/en.json`, `src/i18n/index.ts`
- Test: `tests/i18n.test.ts`

**Interfaces:**
- Produces: `type Locale = 'pt' | 'en'`; `t(locale: Locale): Dict` (dicionário tipado); `altPath(locale: Locale): string` (`/en/` ⇄ `/`); `htmlLang(locale)` (`pt-BR`/`en`).

- [ ] **Step 1: Escrever teste que falha**

`tests/i18n.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { t, altPath, htmlLang } from '../src/i18n';

describe('i18n', () => {
  it('returns dictionaries with mirrored keys', () => {
    const keys = (o: object): string[] =>
      Object.entries(o).flatMap(([k, v]) =>
        v && typeof v === 'object' && !Array.isArray(v) ? keys(v).map((s) => `${k}.${s}`) : [k]);
    expect(keys(t('pt'))).toEqual(keys(t('en')));
  });
  it('altPath swaps locales', () => {
    expect(altPath('pt')).toBe('/en/');
    expect(altPath('en')).toBe('/');
  });
  it('htmlLang maps to BCP47', () => {
    expect(htmlLang('pt')).toBe('pt-BR');
    expect(htmlLang('en')).toBe('en');
  });
});
```

Run: `npx vitest run` — Expected: FAIL (módulo não existe).

- [ ] **Step 2: Criar dicionários**

`src/i18n/pt.json`:
```json
{
  "meta": {
    "title": "Empirika — Software house no Recife",
    "description": "Automação de processos, desenvolvimento de software, websites e dashboards de dados para empresas que crescem."
  },
  "nav": { "capabilities": "Capacidades", "approach": "Método", "contact": "Contato", "switch": "EN", "switchHref": "/en/" },
  "hero": {
    "line1": "Construímos sistemas que trabalham.",
    "line2": "Automação, software e dados para empresas que crescem.",
    "scroll": "Role para explorar"
  },
  "capabilities": {
    "label": "Capacidades",
    "items": [
      { "title": "Automação de processos", "body": "Fluxos manuais viram sistemas. Integrações, filas e robôs que eliminam retrabalho e erro humano." },
      { "title": "Desenvolvimento de software", "body": "Produto sob medida, do MVP ao legado modernizado. Código documentado, entregue no seu repositório." },
      { "title": "Websites", "body": "Sites institucionais rápidos, mensuráveis e desenhados para converter — não só para existir." },
      { "title": "Dashboards de dados", "body": "Seus dados operacionais em painéis que respondem perguntas, não relatórios que ninguém abre." }
    ]
  },
  "approach": {
    "label": "Método",
    "title": "Trabalho sênior, sem caixa preta.",
    "items": [
      { "title": "Direto com quem faz", "body": "Você fala com quem escreve o código. Sem camadas de gerência, sem telefone sem fio." },
      { "title": "Entrega toda semana", "body": "Software em produção desde as primeiras semanas. Progresso que se vê, não status report." },
      { "title": "O código é seu", "body": "Repositório, infraestrutura e documentação no seu nome desde o primeiro commit. Zero lock-in." }
    ]
  },
  "contact": {
    "label": "Contato",
    "title1": "Vamos trabalhar.",
    "title2": "Comece uma conversa.",
    "name": "Nome",
    "email": "E-mail",
    "company": "Empresa (opcional)",
    "message": "Mensagem",
    "submit": "Enviar mensagem",
    "sending": "Enviando…",
    "success": "Mensagem enviada. Respondemos em até um dia útil.",
    "error": "Não foi possível enviar. Tente de novo ou escreva para"
  },
  "footer": {
    "cnpj": "CNPJ 66.436.862/0001-70",
    "address": ["Avenida Rio Branco 139", "Recife, Recife — PE", "50030-310"],
    "rights": "© 2026 Empirika. Todos os direitos reservados."
  }
}
```

`src/i18n/en.json`:
```json
{
  "meta": {
    "title": "Empirika — Software house in Recife, Brazil",
    "description": "Process automation, software development, websites and data dashboards for companies that scale."
  },
  "nav": { "capabilities": "Capabilities", "approach": "Approach", "contact": "Contact", "switch": "PT", "switchHref": "/" },
  "hero": {
    "line1": "We build systems that work.",
    "line2": "Automation, software and data for companies that scale.",
    "scroll": "Scroll to explore"
  },
  "capabilities": {
    "label": "Capabilities",
    "items": [
      { "title": "Process automation", "body": "Manual workflows become systems. Integrations, queues and bots that remove rework and human error." },
      { "title": "Software development", "body": "Custom product work, from MVP to modernized legacy. Documented code, delivered in your repository." },
      { "title": "Websites", "body": "Fast, measurable marketing sites designed to convert — not just to exist." },
      { "title": "Data dashboards", "body": "Your operational data in panels that answer questions, not reports nobody opens." }
    ]
  },
  "approach": {
    "label": "Approach",
    "title": "Senior work, no black box.",
    "items": [
      { "title": "Direct to the makers", "body": "You talk to the people writing the code. No management layers, no broken telephone." },
      { "title": "Shipping every week", "body": "Software in production from the first weeks. Progress you can see, not status reports." },
      { "title": "The code is yours", "body": "Repository, infrastructure and docs under your name from the first commit. Zero lock-in." }
    ]
  },
  "contact": {
    "label": "Contact",
    "title1": "Let's get to work.",
    "title2": "Start a conversation.",
    "name": "Name",
    "email": "Email",
    "company": "Company (optional)",
    "message": "Message",
    "submit": "Send message",
    "sending": "Sending…",
    "success": "Message sent. We reply within one business day.",
    "error": "Could not send. Try again or write to"
  },
  "footer": {
    "cnpj": "CNPJ 66.436.862/0001-70",
    "address": ["Avenida Rio Branco 139", "Recife, Recife — PE", "50030-310"],
    "rights": "© 2026 Empirika. All rights reserved."
  }
}
```

- [ ] **Step 3: Criar helper**

`src/i18n/index.ts`:
```ts
import pt from './pt.json';
import en from './en.json';

export type Locale = 'pt' | 'en';
export type Dict = typeof pt;

const dicts: Record<Locale, Dict> = { pt, en: en as Dict };

export function t(locale: Locale): Dict {
  return dicts[locale];
}

export function altPath(locale: Locale): string {
  return locale === 'pt' ? '/en/' : '/';
}

export function htmlLang(locale: Locale): string {
  return locale === 'pt' ? 'pt-BR' : 'en';
}
```

- [ ] **Step 4: Rodar testes**

Run: `npx vitest run` — Expected: 3 PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: add pt/en dictionaries and i18n helper"
```

---

### Task 3: Layout, Header e Footer

**Files:**
- Create: `src/layouts/Layout.astro`, `src/components/Header.astro`, `src/components/Footer.astro`, `src/pages/en/index.astro`
- Modify: `src/pages/index.astro`

**Interfaces:**
- Consumes: `t`, `altPath`, `htmlLang` de `src/i18n`.
- Produces: `<Layout locale={...}>` com slot; Header fixo com nav âncora (`#capabilities`, `#approach`, `#contact`) e switch de idioma; Footer com CNPJ/endereço.

- [ ] **Step 1: Layout.astro**

```astro
---
import '../styles/global.css';
import { t, altPath, htmlLang, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<html lang={htmlLang(locale)}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{c.meta.title}</title>
    <meta name="description" content={c.meta.description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate" hreflang={locale === 'pt' ? 'en' : 'pt-BR'} href={altPath(locale)} />
    <link rel="alternate" hreflang={htmlLang(locale)} href={locale === 'pt' ? '/' : '/en/'} />
  </head>
  <body class="bg-ink text-paper antialiased">
    <slot />
  </body>
</html>
```

- [ ] **Step 2: Header.astro**

```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<header class="fixed inset-x-0 top-0 z-50 flex items-center justify-between px-6 py-5 md:px-12 mix-blend-difference">
  <a href={locale === 'pt' ? '/' : '/en/'} class="font-display text-sm font-bold uppercase tracking-[0.2em]">Empirika</a>
  <nav class="flex items-center gap-6 font-mono text-xs uppercase tracking-widest">
    <a href="#capabilities" class="hidden text-mute transition-colors hover:text-paper sm:block">{c.nav.capabilities}</a>
    <a href="#approach" class="hidden text-mute transition-colors hover:text-paper sm:block">{c.nav.approach}</a>
    <a href="#contact" class="text-mute transition-colors hover:text-paper">{c.nav.contact}</a>
    <a href={c.nav.switchHref} class="border border-line px-2 py-1 text-mute transition-colors hover:text-paper">{c.nav.switch}</a>
  </nav>
</header>
```

- [ ] **Step 3: Footer.astro**

```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<footer class="border-t border-line px-6 py-16 md:px-12">
  <div class="grid gap-10 font-mono text-xs leading-relaxed text-mute md:grid-cols-3">
    <div>
      <p class="mb-3 uppercase tracking-widest text-paper">Empirika</p>
      <p>{c.footer.cnpj}</p>
    </div>
    <address class="not-italic">
      {c.footer.address.map((line) => <p>{line}</p>)}
    </address>
    <div class="md:text-right">
      <a href={c.nav.switchHref} class="underline underline-offset-4 hover:text-paper">{c.nav.switch}</a>
      <p class="mt-3">{c.footer.rights}</p>
    </div>
  </div>
</footer>
```

- [ ] **Step 4: Páginas nas duas línguas**

`src/pages/index.astro`:
```astro
---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---
<Layout locale="pt">
  <Header locale="pt" />
  <main><h1 class="pt-40 text-center font-display text-6xl">Empirika</h1></main>
  <Footer locale="pt" />
</Layout>
```

`src/pages/en/index.astro`: idêntico com `locale="en"` e imports `../../`.

- [ ] **Step 5: Verificar e commitar**

Run: `npm run build && npm run test` — Expected: verde. Abrir preview e conferir `/` (PT) e `/en/` (EN), CNPJ e endereço no footer.

```bash
git add -A && git commit -m "feat: add layout shell, header and footer with cnpj/address"
```

---

### Task 4: Hero (fase poster — sem vídeo ainda)

**Files:**
- Create: `src/components/Hero.astro`
- Modify: `src/pages/index.astro`, `src/pages/en/index.astro`

**Interfaces:**
- Produces: `<Hero locale={...}>`; section `#hero` com `[data-hero]`, `<canvas data-hero-canvas hidden>` (slot do scrub da Task 8) e headline overlay `[data-hero-copy]`.

- [ ] **Step 1: Hero.astro**

```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<section id="hero" data-hero class="relative flex h-[100svh] items-end overflow-hidden bg-ink">
  <canvas data-hero-canvas hidden class="absolute inset-0 h-full w-full object-cover"></canvas>
  <div data-hero-copy class="relative z-10 w-full px-6 pb-20 md:px-12">
    <h1 class="font-display text-[clamp(2.5rem,7.5vw,7rem)] font-semibold leading-[0.98] tracking-tight">
      <span class="block text-paper">{c.hero.line1}</span>
      <span class="block text-mute">{c.hero.line2}</span>
    </h1>
    <p class="mt-12 font-mono text-xs uppercase tracking-widest text-mute" data-scroll-cue>{c.hero.scroll} ↓</p>
  </div>
</section>
```

- [ ] **Step 2: Substituir o `<main>` stub das duas páginas**

```astro
<main>
  <Hero locale="pt" />
</main>
```
(e `locale="en"` em `/en/`).

- [ ] **Step 3: Verificar e commitar**

Run: `npm run build` — Expected: verde; headline ocupa a dobra nas duas línguas.

```bash
git add -A && git commit -m "feat: add hero section with poster-phase headline"
```

---

### Task 5: Capabilities e Approach

**Files:**
- Create: `src/components/Capabilities.astro`, `src/components/Approach.astro`
- Modify: `src/pages/index.astro`, `src/pages/en/index.astro`

**Interfaces:**
- Produces: sections `#capabilities` e `#approach` com atributos `[data-reveal]` (usados pela Task 7).

- [ ] **Step 1: Capabilities.astro**

```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<section id="capabilities" class="border-t border-line px-6 py-24 md:px-12 md:py-32">
  <p class="font-mono text-xs uppercase tracking-widest text-mute" data-reveal>{c.capabilities.label}</p>
  <ol class="mt-12">
    {c.capabilities.items.map((item, i) => (
      <li class="group border-b border-line py-10 transition-colors hover:bg-paper hover:text-ink md:grid md:grid-cols-12 md:items-baseline md:gap-8 md:px-4" data-reveal>
        <span class="font-mono text-sm text-mute md:col-span-1">0{i + 1}</span>
        <h2 class="mt-2 font-display text-3xl font-semibold tracking-tight md:col-span-5 md:mt-0 md:text-5xl">{item.title}</h2>
        <p class="mt-4 max-w-md text-mute group-hover:text-ink/70 md:col-span-6 md:mt-0">{item.body}</p>
      </li>
    ))}
  </ol>
</section>
```

- [ ] **Step 2: Approach.astro**

```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
---
<section id="approach" class="border-t border-line px-6 py-24 md:px-12 md:py-32">
  <p class="font-mono text-xs uppercase tracking-widest text-mute" data-reveal>{c.approach.label}</p>
  <h2 class="mt-8 max-w-3xl font-display text-4xl font-semibold leading-tight tracking-tight md:text-6xl" data-reveal>{c.approach.title}</h2>
  <div class="mt-16 grid gap-12 md:grid-cols-3" data-parallax-group>
    {c.approach.items.map((item, i) => (
      <div data-reveal>
        <span class="font-mono text-sm text-mute">0{i + 1}</span>
        <h3 class="mt-3 font-display text-xl font-semibold">{item.title}</h3>
        <p class="mt-3 text-mute">{item.body}</p>
      </div>
    ))}
  </div>
</section>
```

- [ ] **Step 3: Montar nas páginas, verificar, commitar**

`<main>` das duas páginas: `Hero`, `Capabilities`, `Approach` (locale correspondente).

Run: `npm run build` — Expected: verde; âncoras do header navegam.

```bash
git add -A && git commit -m "feat: add capabilities manifest rows and approach manifesto"
```

---

### Task 6: Contato — validação (TDD), endpoint Resend e form

**Files:**
- Create: `src/lib/validate-contact.ts`, `src/pages/api/contact.ts`, `src/components/Contact.astro`, `src/scripts/contact-form.ts`
- Test: `tests/validate-contact.test.ts`
- Modify: `src/pages/index.astro`, `src/pages/en/index.astro`

**Interfaces:**
- Produces: `validateContact(data: unknown): { ok: true; value: ContactInput } | { ok: false; errors: string[] }`; `ContactInput = { name; email; message; company?; website? }`; rota `POST /api/contact` → `{ ok: boolean }` (200/400/422/502).

- [ ] **Step 1: Teste de validação que falha**

`tests/validate-contact.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { validateContact } from '../src/lib/validate-contact';

const valid = { name: 'Ana Souza', email: 'ana@empresa.com', company: 'Empresa', message: 'Quero automatizar meu faturamento.' };

describe('validateContact', () => {
  it('accepts a valid payload', () => {
    const r = validateContact(valid);
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.value.email).toBe('ana@empresa.com');
  });
  it('rejects non-object payloads', () => {
    expect(validateContact(null).ok).toBe(false);
    expect(validateContact('x').ok).toBe(false);
  });
  it('rejects bad email and short fields', () => {
    expect(validateContact({ ...valid, email: 'nope' }).ok).toBe(false);
    expect(validateContact({ ...valid, name: 'A' }).ok).toBe(false);
    expect(validateContact({ ...valid, message: 'curta' }).ok).toBe(false);
  });
  it('passes honeypot through untouched', () => {
    const r = validateContact({ ...valid, website: 'http://spam' });
    expect(r.ok && r.value.website).toBe('http://spam');
  });
});
```

Run: `npx vitest run` — Expected: FAIL.

- [ ] **Step 2: Implementar validação**

`src/lib/validate-contact.ts`:
```ts
export interface ContactInput {
  name: string;
  email: string;
  message: string;
  company?: string;
  website?: string; // honeypot — humanos não preenchem
}

type Result = { ok: true; value: ContactInput } | { ok: false; errors: string[] };

const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;

export function validateContact(data: unknown): Result {
  if (typeof data !== 'object' || data === null) return { ok: false, errors: ['payload'] };
  const d = data as Record<string, unknown>;
  const errors: string[] = [];
  const name = typeof d.name === 'string' ? d.name.trim() : '';
  const email = typeof d.email === 'string' ? d.email.trim() : '';
  const message = typeof d.message === 'string' ? d.message.trim() : '';
  const company = typeof d.company === 'string' ? d.company.trim().slice(0, 100) : undefined;
  const website = typeof d.website === 'string' && d.website !== '' ? d.website : undefined;
  if (name.length < 2 || name.length > 100) errors.push('name');
  if (!EMAIL_RE.test(email)) errors.push('email');
  if (message.length < 10 || message.length > 5000) errors.push('message');
  if (errors.length) return { ok: false, errors };
  return { ok: true, value: { name, email, message, company, website } };
}
```

Run: `npx vitest run` — Expected: PASS.

- [ ] **Step 3: Endpoint**

`src/pages/api/contact.ts`:
```ts
export const prerender = false;
import type { APIRoute } from 'astro';
import { Resend } from 'resend';
import { validateContact } from '../../lib/validate-contact';

const json = (body: object, status = 200) =>
  new Response(JSON.stringify(body), { status, headers: { 'Content-Type': 'application/json' } });

export const POST: APIRoute = async ({ request }) => {
  let data: unknown;
  try {
    data = await request.json();
  } catch {
    return json({ ok: false }, 400);
  }
  const result = validateContact(data);
  if (!result.ok) return json({ ok: false, errors: result.errors }, 422);
  if (result.value.website) return json({ ok: true }); // honeypot: finge sucesso
  const resend = new Resend(import.meta.env.RESEND_API_KEY);
  const { error } = await resend.emails.send({
    from: import.meta.env.CONTACT_FROM ?? 'Empirika <onboarding@resend.dev>',
    to: import.meta.env.CONTACT_TO ?? 'paulinhoerry@gmail.com',
    replyTo: result.value.email,
    subject: `[site] ${result.value.name}${result.value.company ? ' — ' + result.value.company : ''}`,
    text: `Nome: ${result.value.name}\nE-mail: ${result.value.email}\nEmpresa: ${result.value.company ?? '-'}\n\n${result.value.message}`,
  });
  if (error) {
    console.error('[contact] resend error', error);
    return json({ ok: false }, 502);
  }
  return json({ ok: true });
};
```

- [ ] **Step 4: Form + script cliente**

`src/scripts/contact-form.ts`:
```ts
export function initContactForm(form: HTMLFormElement): void {
  const status = form.querySelector<HTMLElement>('[data-status]');
  const btn = form.querySelector<HTMLButtonElement>('button[type="submit"]');
  if (!status || !btn) return;
  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    btn.disabled = true;
    status.textContent = status.dataset.sending ?? '';
    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(Object.fromEntries(new FormData(form).entries())),
      });
      const body = (await res.json()) as { ok: boolean };
      if (!res.ok || !body.ok) throw new Error('send_failed');
      form.reset();
      status.textContent = status.dataset.success ?? '';
    } catch {
      status.textContent = `${status.dataset.error ?? ''} ${status.dataset.mailto ?? ''}`.trim();
    } finally {
      btn.disabled = false;
    }
  });
}
```

`src/components/Contact.astro`:
```astro
---
import { t, type Locale } from '../i18n';
interface Props { locale: Locale }
const { locale } = Astro.props;
const c = t(locale);
const mailto = 'paulinhoerry@gmail.com';
---
<section id="contact" class="border-t border-line px-6 py-24 md:px-12 md:py-32">
  <p class="font-mono text-xs uppercase tracking-widest text-mute" data-reveal>{c.contact.label}</p>
  <h2 class="mt-8 font-display text-[clamp(2.5rem,6vw,5.5rem)] font-semibold leading-[1.02] tracking-tight" data-reveal>
    <span class="block text-paper">{c.contact.title1}</span>
    <span class="block text-mute">{c.contact.title2}</span>
  </h2>
  <form data-contact-form novalidate class="mt-16 grid max-w-2xl gap-8" data-reveal>
    <input type="text" name="website" tabindex="-1" autocomplete="off" aria-hidden="true" class="hidden" />
    <label class="grid gap-2 font-mono text-xs uppercase tracking-widest text-mute">
      {c.contact.name}
      <input required minlength="2" maxlength="100" name="name" type="text"
        class="border-b border-line bg-transparent py-3 font-display text-lg normal-case tracking-normal text-paper outline-none transition-colors focus:border-paper" />
    </label>
    <label class="grid gap-2 font-mono text-xs uppercase tracking-widest text-mute">
      {c.contact.email}
      <input required name="email" type="email"
        class="border-b border-line bg-transparent py-3 font-display text-lg normal-case tracking-normal text-paper outline-none transition-colors focus:border-paper" />
    </label>
    <label class="grid gap-2 font-mono text-xs uppercase tracking-widest text-mute">
      {c.contact.company}
      <input maxlength="100" name="company" type="text"
        class="border-b border-line bg-transparent py-3 font-display text-lg normal-case tracking-normal text-paper outline-none transition-colors focus:border-paper" />
    </label>
    <label class="grid gap-2 font-mono text-xs uppercase tracking-widest text-mute">
      {c.contact.message}
      <textarea required minlength="10" maxlength="5000" name="message" rows="5"
        class="resize-y border-b border-line bg-transparent py-3 font-display text-lg normal-case tracking-normal text-paper outline-none transition-colors focus:border-paper"></textarea>
    </label>
    <button type="submit"
      class="justify-self-start border border-paper px-8 py-4 font-mono text-xs uppercase tracking-widest transition-colors hover:bg-paper hover:text-ink disabled:opacity-50">
      {c.contact.submit}
    </button>
    <p aria-live="polite" data-status data-sending={c.contact.sending} data-success={c.contact.success}
      data-error={c.contact.error} data-mailto={mailto} class="min-h-6 font-mono text-xs text-mute"></p>
  </form>
  <p class="mt-10 font-mono text-sm text-mute">
    <a href={`mailto:${mailto}`} class="underline underline-offset-4 hover:text-paper">{mailto}</a>
  </p>
</section>

<script>
  import { initContactForm } from '../scripts/contact-form';
  document.querySelectorAll<HTMLFormElement>('[data-contact-form]').forEach(initContactForm);
</script>
```

- [ ] **Step 5: Montar, verificar, commitar**

Adicionar `<Contact locale=... />` como última section do `<main>` nas duas páginas.

Run: `npm run test && npm run build` — Expected: verde. Com `npm run dev` + `RESEND_API_KEY` de teste em `.env`, submeter o form e conferir resposta JSON.

```bash
git add -A && git commit -m "feat: add resend contact endpoint, validated form with honeypot"
```

---

### Task 7: Animações GSAP (reveals + parallax + reduced motion)

**Files:**
- Create: `src/scripts/animations.ts`
- Modify: `src/layouts/Layout.astro`

**Interfaces:**
- Consumes: atributos `[data-reveal]`, `[data-parallax-group]`, `[data-scroll-cue]` das Tasks 4–6.
- Produces: `initAnimations(): void`, chamado uma vez no Layout.

- [ ] **Step 1: animations.ts**

```ts
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

export function initAnimations(): void {
  const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (reduced) {
    document.querySelectorAll<HTMLElement>('[data-reveal]').forEach((el) => (el.style.opacity = '1'));
    return;
  }
  gsap.registerPlugin(ScrollTrigger);

  document.querySelectorAll<HTMLElement>('[data-reveal]').forEach((el) => {
    gsap.fromTo(
      el,
      { opacity: 0, y: 32 },
      {
        opacity: 1, y: 0, duration: 0.8, ease: 'power2.out',
        scrollTrigger: { trigger: el, start: 'top 85%', once: true },
      },
    );
  });

  document.querySelectorAll<HTMLElement>('[data-parallax-group]').forEach((group) => {
    Array.from(group.children).forEach((child, i) => {
      gsap.to(child, {
        y: () => -16 * (i + 1),
        ease: 'none',
        scrollTrigger: { trigger: group, start: 'top bottom', end: 'bottom top', scrub: 0.6 },
      });
    });
  });

  const cue = document.querySelector('[data-scroll-cue]');
  if (cue) {
    gsap.to(cue, { opacity: 0, scrollTrigger: { trigger: '#hero', start: 'top top', end: '30% top', scrub: true } });
  }
}
```

- [ ] **Step 2: Chamar no Layout**

Antes de `</body>` no `Layout.astro`:
```astro
<script>
  import { initAnimations } from '../scripts/animations';
  initAnimations();
</script>
```

- [ ] **Step 3: Verificar e commitar**

Run: `npm run build` — Expected: verde. No preview: reveals disparam ao rolar; com "Emulate prefers-reduced-motion" no devtools tudo aparece sem animação.

```bash
git add -A && git commit -m "feat: add gsap scroll reveals and parallax with reduced-motion guard"
```

---

### Task 8: Hero scroll-world (Higgsfield)

**Files:**
- Create: `public/hero/` (frames/poster gerados), `src/scripts/hero-scrub.ts` (adaptado de `references/scrub-engine.js` da skill)
- Modify: `src/components/Hero.astro`, `src/layouts/Layout.astro` (se a skill pedir preload)

**Interfaces:**
- Consumes: `<canvas data-hero-canvas>` e `[data-hero-copy]` da Task 4; Higgsfield CLI autenticado (workspace Private, 210 créditos); ffmpeg.
- Produces: hero scrubado pelo scroll em 1080p sem áudio, com poster fallback e bypass em reduced-motion.

- [ ] **Step 1: Seguir a skill scroll-world**

A skill clonada está em `<scratchpad>/scroll-world/skills/scroll-world/SKILL.md`. Segui-la fielmente (Step 0 em diante) com o brief:
"Empirika — software house in Recife, Brazil. Scene chain: modern dev office with code screens → abstract automation flow (pipes/queues/robots) → glowing data dashboard control room → Recife port with river and bridges at dusk. Dark, cinematic, enterprise mood; 1080p landscape; no audio."
Antes do lote: calibrar custo real de 1 imagem + 1 vídeo no CLI e confirmar que os 210 créditos cobrem N=4 cenas (4 imagens + 7 vídeos); se não cobrirem, reduzir para N=3 e avisar o Paulo.

- [ ] **Step 2: Integrar o output no Hero**

Copiar frames extraídos para `public/hero/frames/`, poster para `public/hero/poster.jpg`. Adaptar o `scrub-engine.js` da skill para `src/scripts/hero-scrub.ts` exportando `initHeroScrub(section: HTMLElement): void`, e chamar num `<script>` do `Hero.astro`. Reduced-motion e falha de fetch de frames: mostrar `poster.jpg` como background do canvas e retornar.

- [ ] **Step 3: Verificar e commitar**

Run: `npm run build` — Expected: verde; scrub suave no preview em 1440px; poster em reduced-motion; página funciona com `public/hero/frames` renomeado (fallback).

```bash
git add -A && git commit -m "feat: wire scroll-world hero with higgsfield frames and poster fallback"
```

---

### Task 9: Refinamento impeccable + QA final + README

**Files:**
- Create: `README.md`
- Modify: qualquer componente que o passe de crítica apontar

**Interfaces:**
- Consumes: site completo das Tasks 1–8; skill impeccable em `.agents/skills/impeccable/`.

- [ ] **Step 1: Passe impeccable**

Ler `.agents/skills/impeccable/SKILL.md` e rodar os passes de crítica/refinamento (`critique`, `typeset`, `polish`) sobre as seções; aplicar os ajustes de tipografia/espaçamento apontados.

- [ ] **Step 2: Smoke test Playwright (MCP)**

Com `npm run preview` rodando, usar o browser Playwright para verificar em `/` e `/en/`: headline do hero visível; 4 linhas de capabilities; manifesto; form rejeita e-mail inválido (validação nativa); switch de idioma alterna URL; footer contém `CNPJ 66.436.862/0001-70` e `50030-310`. Testar larguras 1440/768/390.

- [ ] **Step 3: README**

`README.md` com: setup (`npm install`, Node ≥ 20), scripts, envs (`RESEND_API_KEY`, `CONTACT_TO`, `CONTACT_FROM`), como regenerar o hero (skill scroll-world + Higgsfield), deploy Vercel.

- [ ] **Step 4: Verificação final e commit**

Run: `npm run test && npm run build` — Expected: tudo verde.

```bash
git add -A && git commit -m "docs: add readme; polish pass across sections"
```
